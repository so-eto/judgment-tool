return (
    <div className="min-h-screen bg-slate-50 text-slate-800 font-sans flex flex-col antialiased">
      
      {/* プレミアムなヘッダー */}
      <header className="bg-gradient-to-r from-slate-900 via-indigo-950 to-slate-900 text-white shadow-md border-b border-indigo-900 py-6 px-6">
        <div className="max-w-6xl mx-auto flex flex-col md:flex-row justify-between items-start md:items-center gap-4">
          <div>
            <div className="flex items-center gap-2">
              <span className="bg-gradient-to-r from-emerald-500 to-teal-500 text-white text-[10px] font-extrabold px-2.5 py-1 rounded-md uppercase tracking-wider shadow-md">
                HIGH SPEED HYBRID
              </span>
              <h1 className="text-2xl font-bold tracking-tight">前株・後株 一括スピード判別アシスタント</h1>
            </div>
            <p className="text-slate-300 text-sm mt-1.5 leading-relaxed max-w-2xl">
              ローカル超高速キャッシュ＆Gemini AIの連携システム。よく使われる有名企業は<b>秒速0秒</b>で即座に特定し、新規企業もバックグラウンドで並列解析します。
            </p>
          </div>
          <button 
            onClick={() => setInputText(`アシックス\nトヨタ\n任天堂\nキーエンス\nメルカリ`)}
            className="text-xs text-indigo-300 hover:text-white transition-colors bg-white/5 border border-white/10 hover:bg-white/10 rounded-lg px-3 py-1.5 self-stretch md:self-auto text-center font-semibold"
          >
            入力例を読み直す
          </button>
        </div>
      </header>

      {/* メインレイアウト */}
      <main className="max-w-6xl w-full mx-auto p-4 md:p-6 grid grid-cols-1 lg:grid-cols-12 gap-6 flex-grow">
        
        {/* 左側：企業名一括入力パネル (5/12幅) */}
        <div className="lg:col-span-5 flex flex-col gap-4">
          <div className="bg-white rounded-2xl shadow-sm border border-slate-200 p-5 flex flex-col flex-grow min-h-[440px]">
            <div className="flex justify-between items-center mb-3">
              <label className="text-sm font-bold text-slate-700 flex items-center gap-1.5">
                <span className="w-2.5 h-2.5 rounded-full bg-indigo-500"></span>
                企業名を複数行で入力（コピペ可）
              </label>
              <button 
                onClick={() => setInputText('')}
                className="text-xs text-slate-400 hover:text-rose-500 font-bold transition-colors"
              >
                クリア
              </button>
            </div>
            
            <textarea
              className="w-full flex-grow p-4 bg-slate-50 border border-slate-200 rounded-xl text-sm font-medium text-slate-800 placeholder-slate-400 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:bg-white transition-all resize-none leading-relaxed shadow-inner"
              placeholder={`アシックス\nトヨタ\n任天堂\nキーエンス\nメルカリ\nのように改行区切りで入力してください...`}
              value={inputText}
              onChange={(e) => setInputText(e.target.value)}
            />
            
            <div className="mt-4 flex items-center justify-between">
              <span className="text-xs text-slate-400 font-medium">
                入力件数: <strong className="text-indigo-600 font-bold">{inputText.split('\n').filter(Boolean).length}</strong> 件
              </span>
              
              {/* 「結果を確認する」トリガーボタン */}
              <button
                onClick={handleVerify}
                disabled={isProcessing || !inputText.trim()}
                className={`py-3 px-6 rounded-xl font-bold text-sm shadow-md transition-all flex items-center gap-2 ${
                  inputText.trim() 
                    ? 'bg-indigo-600 hover:bg-indigo-500 text-white active:scale-95 cursor-pointer' 
                    : 'bg-slate-100 text-slate-400 cursor-not-allowed border border-slate-200'
                }`}
              >
                {isProcessing ? (
                  <>
                    <span className="w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                    AI解析中...
                  </>
                ) : (
                  <>
                    <IconSparkles />
                    結果を確認する
                  </>
                )}
              </button>
            </div>
          </div>
        </div>

        {/* 右側：判別結果一覧 ＆ 一括コピーパネル (7/12幅) */}
        <div className="lg:col-span-7 flex flex-col gap-4">
          
          {apiError && (
            <div className="bg-rose-50 border border-rose-200 p-4 rounded-xl flex items-start gap-3">
              <IconWarning />
              <div>
                <h4 className="text-sm font-bold text-rose-800">API通信エラー</h4>
                <p className="text-xs text-rose-700 mt-1 leading-relaxed">{apiError}</p>
              </div>
            </div>
          )}
